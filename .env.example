# Copy this file to .env and customize values

# Public-facing domain (for email/links). For local dev, leave as localhost.
CANVAS_LMS_DOMAIN=localhost
CANVAS_LMS_TIME_ZONE=UTC
CANVAS_LMS_ACCOUNT_NAME=Aurnova University

# Initial admin user
CANVAS_LMS_ADMIN_EMAIL=admin@localhost
CANVAS_LMS_ADMIN_PASSWORD=changeme123
CANVAS_LMS_ADMIN_NAME=Admin

# Generate with: openssl rand -hex 64
SECRET_KEY_BASE=
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
